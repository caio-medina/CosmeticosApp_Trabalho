<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="a2017_1.pooa.iff.br.cosmeticosapp_trabalho">

    <application
        android:name=".persistence.BDConfig"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.LoginActivity" />
        <activity android:name=".model.Menu_principal" />
        <activity android:name=".model.Corpo" />
        <activity android:name=".model.Rosto" />
        <activity android:name=".model.Carrinho"></activity>
    </application>

</manifest>